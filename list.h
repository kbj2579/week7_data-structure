#ifndef LIST_H
#define LIST_H

#include "linked_list.h"
using namespace std;
// TODO: List 클래스 정의 작성

class list{
    
    private: 
        void insert(int index, int value);
        int get(int index);
        void remove(int index);
}

#endif